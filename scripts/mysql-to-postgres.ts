/**
 * Stub — convert MySQL dump from สพป.ชัยนาท production to PostgreSQL format.
 * UAT/go-live: run before db:load-legacy on staging server.
 *
 * Usage: tsx scripts/mysql-to-postgres.ts input.sql output.sql
 */
console.log(
  "mysql-to-postgres: not implemented yet. Use pgloader or manual conversion for UAT.",
);
process.exit(0);
